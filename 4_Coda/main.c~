#include <stdio.h>
#include "mialib.h"

int main(){
	
	Coda coda;
	
	printf("inizializzo \n\n");
	inizializza(&coda);
	
	printf("inserisco valori \n\n");
	push(&coda,1);
	push(&coda,1);
	push(&coda,1);
	push(&coda,1);
	push(&coda,1);
	
	printf("stampo la coda \n\n");
	stampa(&coda);
	
	return 0;
}
